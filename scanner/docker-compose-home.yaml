services:
  home:
    build:
      context: ..
      dockerfile: scanner/Dockerfile-tests
    networks:
      - tests
    environment:
      - TARGET_NETWORK=192.168.0.0/16
      - SELF_IP=192.168.1.128
      - ROUTER_IP=192.168.1.1
      - SCAN_TEST_ENV=HOME
    command:
      - sleep
      - inf
networks:
  tests:
    driver: bridge
